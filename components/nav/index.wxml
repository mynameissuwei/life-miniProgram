<!--components/nav/index.wxml-->
<view class="container">
  <image src="{{lastest?disleft:left}}" bindtap='onLeft'></image>
  <text>{{title}}</text>
  <image src="{{oldest?disright:right}}" bindtap='onRight'></image>
</view>
